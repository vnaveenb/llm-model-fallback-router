# Model Fallback Router — Environment Variables

# LLM Provider Keys (add keys for providers you configure in config.yaml)
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Tracker
TRACKER_DB_PATH=./data/usage.db

# Overrides (optional)
# ROUTING_STRATEGY=priority
# INFERENCE_MOCK=true
# SERVER_PORT=8400
# CONFIG_PATH=config.yaml
